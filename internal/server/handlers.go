package server

import (
	"github.com/gin-gonic/gin"
)

// CreateTaskHandler обработчик для создания задачи
func (s *Server) CreateTaskHandler(c *gin.Context) {
	// Реализация создания задачи
}

// ListTasksHandler обработчик для отображения списка задач
func (s *Server) ListTasksHandler(c *gin.Context) {
	// Реализация отображения задач
}
